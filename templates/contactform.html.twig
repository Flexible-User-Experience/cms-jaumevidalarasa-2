{% extends "base.html.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col">
                {{ form(form) }}
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ dump(page) }}
            </div>
        </div>
    </div>
{% endblock %}
