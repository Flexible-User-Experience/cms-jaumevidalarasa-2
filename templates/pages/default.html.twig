{% extends "base.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col">
            <h2 class="mt-4 mb-4">{{ content.title }}</h2>
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{ content.article | raw }}
        </div>
    </div>
{% endblock %}
